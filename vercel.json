{
  "buildCommand": "npx expo export -p web --project-root mobile",
  "outputDirectory": "mobile/dist",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}